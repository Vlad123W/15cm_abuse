<Window x:Class="_15cm_abuse.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxml.formats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_15cm_abuse"
        Title="Artillery Calculator" Height="600" Width="800"
        WindowStyle="None" BorderBrush="Transparent" AllowsTransparency="True" Background="Transparent"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="#FF2D2D30"/>
        <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="#FF3F3F41"/>
        <SolidColorBrush x:Key="PrimaryBorderBrush" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="TextBrush" Color="#FFF1F1F1"/>
        <SolidColorBrush x:Key="ButtonHoverBrush" Color="#FF3E3E40"/>
        <SolidColorBrush x:Key="ButtonPressedBrush" Color="#FF005A9E"/>
        <SolidColorBrush x:Key="CloseButtonHoverBrush" Color="#FFE81123"/>

        <Style x:Key="ControlButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0" SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonPressedBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ButtonPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource CloseButtonHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{StaticResource PrimaryBorderBrush}" CornerRadius="0" BorderThickness="1" Background="{StaticResource PrimaryBackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid x:Name="TitleBar" Grid.Row="0" Background="#FF3C3C3C" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock Text="Artillery Calculator"
                           Foreground="{StaticResource TextBrush}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Margin="10,0,0,0"
                           FontSize="14"/>
                <Button x:Name="closeBtn"
                        Style="{StaticResource CloseButton}"
                        Content="✕"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Click="CloseBtn_Click"/>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{StaticResource SecondaryBackgroundBrush}" Margin="5">
                    <StackPanel VerticalAlignment="Top" Margin="15,15,15,0">

                        <!-- ComboBox для выбора фото -->
                        <TextBlock Text="Select image:" Foreground="{StaticResource TextBrush}" FontSize="14" Margin="0,0,0,5"/>
                        <ComboBox x:Name="imageSelector"
                  Foreground="Black"
                  Background="{StaticResource PrimaryBackgroundBrush}"
                  BorderBrush="{StaticResource PrimaryBorderBrush}"
                  FontSize="14"
                  Margin="0,0,0,20"
                  SelectionChanged="ImageSelector_SelectionChanged">
                            <ComboBoxItem Content="Map 1" Foreground="Black"/>
                            <ComboBoxItem Content="Map 2" Foreground="Black"/>
                            <ComboBoxItem Content="Map 3" Foreground="Black"/>
                        </ComboBox>

                        <!-- Дистанция -->
                        <TextBlock Text="Distance:" Foreground="{StaticResource TextBrush}" FontSize="16"/>
                        <TextBlock x:Name="distLabel" Text="0 m" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,5,0,20"/>

                        <!-- Азимут -->
                        <TextBlock Text="Azimuth:" Foreground="{StaticResource TextBrush}" FontSize="16"/>
                        <TextBlock x:Name="azimLabel" Text="0°" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,5,0,30"/>

                        <!-- Кнопки -->
                        <Button x:Name="pasteBtn" Style="{StaticResource ControlButton}" Content="Paste" Margin="0,5" Click="PasteBtn_Click"/>
                        <Button x:Name="resetBtn" Style="{StaticResource ControlButton}" Content="Reset" Margin="0,5" Click="ResetBtn_Click"/>
                        <Button x:Name="setScale" Style="{StaticResource ControlButton}" Content="Set scale" Margin="0,5" Click="SetScale_Click"/>

                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Margin="5" BorderBrush="{StaticResource SecondaryBackgroundBrush}" BorderThickness="1" ClipToBounds="True" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Canvas x:Name="imgCanvas">
                        <Image x:Name="img"
                               MouseWheel="Img_MouseWheel"
                               MouseLeftButtonDown="Img_MouseLeftButtonDown"
                               Stretch="Uniform">
                            <Image.RenderTransform>
                                <ScaleTransform x:Name="ImageScale" ScaleX="1" ScaleY="1" CenterX="0.5" CenterY="0.5" />
                            </Image.RenderTransform>
                        </Image>
                    </Canvas>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>